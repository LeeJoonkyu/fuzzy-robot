#c언어 개발 때 자주하는 실수

0. 15! 저장을 int로 하게되면, 32byte signed의 경우 31byte 2^31-1 까지밖에 저장을 못한다
그런데 15!은 오버플로우 문제.

1. 실수 저장 방식의 문제. 실수를 많이 쓰면 실수한다.
2. 매크로 사용 형식의 문제
#define square(a) a*a 보다 (a)*(a)여야.

3. for(i=0;i<strlen(a);i++) 를 피해라
length = strlen(a)로 하는 게 낫다.
4. 배열크기는 상수로 잡을 것. 
